import { LockedFeature } from "@/ui/components/primitives/LockedFeature";

export default function StorePage() {
  return (
    <LockedFeature
      title="SYSTEM STORE"
      unlocksAt="Unlocks once the Army Rank exists — Phase 5."
    />
  );
}
