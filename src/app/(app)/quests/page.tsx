import { LockedFeature } from "@/ui/components/primitives/LockedFeature";

export default function QuestsPage() {
  return (
    <LockedFeature
      title="QUEST LOG"
      unlocksAt="The Daily Quest system arrives in Phase 2."
    />
  );
}
