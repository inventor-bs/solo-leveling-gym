import { LockedFeature } from "@/ui/components/primitives/LockedFeature";

export default function SkillsPage() {
  return (
    <LockedFeature
      title="SKILLS"
      unlocksAt="Unlocks after the Job Change Quest — Phase 6."
    />
  );
}
